import React from 'react';

const Container = (props) => {

    return (
        <div>
            <container>
                {props.children}
            </container>
        </div>

    )
}
export default Container